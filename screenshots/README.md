# Screenshots

Add product screenshots here for the main README (e.g. `dashboard.png`, `simulator.png`, `agent-editor.png`, `settings.png`).

Until then, the README uses temporary placeholder images.
